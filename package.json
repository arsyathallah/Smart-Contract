{
  "name": "simple-voting-smart-contract",
  "version": "1.0.0",
  "description": "Sistem voting on-chain menggunakan Solidity dan Hardhat untuk pemilihan (ketua kelas, proposal, dll)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "interact": "hardhat run scripts/interact.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "keywords": ["solidity", "hardhat", "voting", "blockchain", "smart-contract"],
  "license": "MIT"
}
